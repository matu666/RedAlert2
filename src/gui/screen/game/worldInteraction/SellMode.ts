/**
 * Interaction mode for selling buildings and units
 */
export class SellMode {
  constructor(private game: any, private player: any) {}

  enter(): void {
    // Enter sell mode
  }

  exit(): void {
    // Exit sell mode
  }

  handleClick(target: any): void {
    // Handle sell target selection
  }

  dispose(): void {
    // Cleanup
  }
}
